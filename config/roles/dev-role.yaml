kind: role
version: v7
metadata:
  name: dev
  description: "Grants SSH access as 'develop' to servers of type 'attico'"
spec:
  options:
    max_session_ttl: 8h
    forward_agent: true

  allow:
    logins:
      - develop

    node_labels:
      server-type: "attico"
